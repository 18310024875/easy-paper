// const modules = import.meta.glob(`./**/index.js`, { eager: true, import: 'default' })

// const configList = Object.values(modules).filter(module => module.name && module.widget)

// export default configList

import vBtn from './v-btn'
import vEcahrt from './e-chart'

export default [vBtn, vEcahrt]
